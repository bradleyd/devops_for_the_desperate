apiVersion: v1
kind: Service
metadata:
  labels:
    app: telnet-server
  name: telnet-server-metrics
spec:
  ports:
  - name: metrics
    port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: telnet-server
  type: ClusterIP
status:
  loadBalancer: {}
